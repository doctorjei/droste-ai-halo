# distrobox.ini — droste-llama-halo, distrobox-lane example.
# Create with:   distrobox assemble create --file distrobox.ini
# (remote inis work too: distrobox assemble create --file <URL>)
#
# UNIFIED LANE: init_hooks runs the shared resolver IN the box on every
# container start, giving distrobox the same mount treatment as the server
# lane. In-container it mounts the venv overlay (upper on /opt/program-cache
# — your pip installs survive box recreation; kernel overlay → fuse-overlayfs
# → copy fallback chain, the overlay filesystem rule applies). The host home
# is auto-bound, so the shared HF cache (~/.cache/huggingface) — where -hf
# downloads land, CRITICAL — persists natively and is deliberately NOT
# resolver-mounted. The hook also verifies that critical, seeds
# /opt/data/llama.cfg (edit it, set LLAMA_ARG_MODEL), and writes the
# /opt/models notice. Binds kept in this ini = data + program cache
# (+ optional models) ONLY.
# Serve with:
#   server_start
# and NOT with a bare `set -a; . /opt/data/llama.cfg; set +a; llama-server …`.
# Sourcing the file by hand looks like the same thing and is not. It emits no
# --host and no --port, so your DROSTE_LLAMA_HOST and DROSTE_LLAMA_PORT go
# unread — bare like that it binds llama-server's own default, 127.0.0.1
# (common/common.h:606), narrower than this box's 0.0.0.0, never wider. It also
# skips the blank-is-unset pass, and that one bites: llama.cfg's Reserved block
# says do not set LLAMA_ARG_HOST, and the way a careful reader obeys is to write
# `LLAMA_ARG_HOST=` — an empty host leaves cpp-httplib with no node and no
# AI_PASSIVE, so the server binds ::1 ONLY, the health probe asks 127.0.0.1 and
# gets nothing, and the box restart-loops while looking healthy from inside.
# And `$DROSTE_LLAMA_EXTRA_ARGS` unquoted is word-split AND GLOBBED by the
# shell, where the launcher splits it quote-aware. `server_start` is the lane
# that applies what you actually configured. (Nothing can be read from
# /opt/data/llama.cfg here anyway: this file is edited before the box exists, so
# any literal printed would be a guess — and the wrong guess is the one that
# binds wider than you asked for.)
#
# EDIT the host-side (left) paths; create the host dirs first. They MUST be
# absolute: a source that does not start with / or ./ is read as a NAMED
# VOLUME, not a bind (podman), and distrobox 2.x expands nothing. The
# /home/kirobo below is a stand-in for YOUR home.

[droste-llama]
image=ghcr.io/doctorjei/droste-llama-halo:latest
init_hooks="/opt/resources/resolve/droste-init-hook.sh"
# In-box mounting needs CAP_SYS_ADMIN (+ /dev/fuse for the fuse-overlayfs
# fallback). `additional_flags` is the documented distrobox-assemble key for
# extra container-manager flags (string_list, space-separated).
additional_flags="--cap-add sys_admin --device /dev/fuse"
# /opt/data = per-box PERSISTENT state, never wiped (the persisted llama.cfg).
# /opt/program-cache = per-box PROGRAM CACHE the installer may empty on
# request (venv upper + the saved-prompt slot store).
# Shared compute caches across ALL droste boxes are appended below (optional — remove to keep caches per-box).
volume="/home/kirobo/droste/data/llama:/opt/data /home/kirobo/droste/caches/llama:/opt/program-cache /home/kirobo/droste/compute-caches:/opt/caches"
# Optional read-only local GGUF collection (LLAMA_ARG_MODEL may point here): to enable, APPEND
#   /home/kirobo/models:/opt/models:ro
# (space-separated) INSIDE the single volume= value above — do NOT add a second volume= line
# (distrobox reads only the LAST volume= key, so a second line silently drops the others).
