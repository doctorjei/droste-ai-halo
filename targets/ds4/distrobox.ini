# distrobox.ini — droste-ds4-halo, distrobox-lane example.
# Create with:   distrobox assemble create --file distrobox.ini
# (remote inis work too: distrobox assemble create --file <URL>)
#
# UNIFIED LANE: init_hooks runs the shared resolver IN the box on every
# container start, giving distrobox the same mount treatment as the server
# lane. In-container it mounts the venv overlay (upper on /opt/program-cache
# — your pip installs survive box recreation; kernel overlay → fuse-overlayfs
# → copy fallback chain, the overlay filesystem rule applies), puts the KV
# disk on /opt/program-cache too, and surfaces ~/.ds4 from /opt/data/internal —
# saved sessions land on the data volume (and survive box recreation, and a
# cache wipe) instead of the raw home dir. The host home is auto-bound, so
# the shared HF cache (~/.cache/huggingface) — where download_model.sh
# fetches the 80-430 GB quants, CRITICAL — persists natively and is
# deliberately NOT resolver-mounted. The hook also verifies that critical,
# seeds /opt/data/ds4.cfg (the box's own settings; their DROSTE_DS4_*
# translation runs in THIS lane too — the hook applies the spec and, if
# DROSTE_DS4_STARTUP_ENABLED turns serving on, launches ds4-server right here —
# while the cockpit symlink stays server-lane-only: cockpit state rides the host
# home natively), and writes the /opt/models notice. Binds kept in this ini =
# data + program cache (+ optional models) ONLY.
# Serve interactively with plain flags. No --host and no --port here, on
# purpose: those are DROSTE_DS4_HOST and DROSTE_DS4_PORT in /opt/data/ds4.cfg,
# which does not exist yet when you edit THIS file — so a literal printed here
# would be a guess that quietly contradicted your own settings, and the wrong
# guess is the one that binds WIDER than you asked for.
#   ds4-server -m <model>.gguf   (or ds4-cockpit)
# Bare like that it binds ds4-server's own default, 127.0.0.1 — narrower than
# this box's 0.0.0.0, never wider. `server_start` is the one that applies what
# you configured.
#
# EDIT the host-side (left) paths; create the host dirs first. They MUST be
# absolute: a source that does not start with / or ./ is read as a NAMED
# VOLUME, not a bind (podman), and distrobox 2.x expands nothing. The
# /home/kirobo below is a stand-in for YOUR home.

[droste-ds4]
image=ghcr.io/doctorjei/droste-ds4-halo:latest
init_hooks="/opt/resources/resolve/droste-init-hook.sh"
# In-box mounting needs CAP_SYS_ADMIN (+ /dev/fuse for the fuse-overlayfs
# fallback). `additional_flags` is the documented distrobox-assemble key for
# extra container-manager flags (string_list, space-separated).
additional_flags="--cap-add sys_admin --device /dev/fuse"
# /opt/data = per-box PERSISTENT state, never wiped (the persisted ds4.cfg,
# the ~/.ds4 tree of saved sessions, cockpit settings when serving).
# /opt/program-cache = per-box PROGRAM CACHE the installer may empty on
# request (venv upper + the KV disk).
# Shared compute caches across ALL droste boxes are appended below (optional — remove to keep caches per-box).
volume="/home/kirobo/droste/data/ds4:/opt/data /home/kirobo/droste/caches/ds4:/opt/program-cache /home/kirobo/droste/compute-caches:/opt/caches"
# Optional read-only local GGUF collection (DROSTE_DS4_MODEL may point here): to enable, APPEND
#   /home/kirobo/models:/opt/models:ro
# (space-separated) INSIDE the single volume= value above — do NOT add a second volume= line
# (distrobox reads only the LAST volume= key, so a second line silently drops the others).
