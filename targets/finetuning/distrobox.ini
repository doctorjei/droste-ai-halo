# distrobox.ini — droste-finetuning-halo, distrobox-lane example.
# Create with:   distrobox assemble create --file distrobox.ini
# (remote inis work too: distrobox assemble create --file <URL>)
#
# UNIFIED LANE: init_hooks runs the shared resolver IN the box on every
# container start, giving distrobox the same mount treatment as the server
# lane. In-container it mounts the venv overlay (upper on /opt/program
# — your pip installs survive box recreation; kernel overlay → fuse-overlayfs
# → copy fallback chain, the overlay filesystem rule applies) and binds the
# compute caches (/opt/program-cache/compute/{miopen,miopen-db,triton,torch}
# over their ~/. paths, so box-specific kernel caches live with the box's
# disposables instead of leaking into the real home). The host home is
# auto-bound, so the shared HF cache (~/.cache/huggingface, CRITICAL)
# persists natively and is deliberately NOT resolver-mounted. The hook also
# verifies the CRITICAL binds below and seeds the starter notebooks into an
# EMPTY workspace. Binds kept in this ini = config + program + program cache
# + workspace ONLY.
#
# EDIT the host-side (left) paths; create the host dirs first. They MUST be
# absolute: a source that does not start with / or ./ is read as a NAMED
# VOLUME, not a bind (podman), and distrobox 2.x expands nothing. The
# /home/kirobo below is a stand-in for YOUR home.

[droste-finetuning]
image=ghcr.io/doctorjei/droste-finetuning-halo:latest
init_hooks="/opt/resources/resolve/droste-init-hook.sh"
# In-box mounting needs CAP_SYS_ADMIN (+ /dev/fuse for the fuse-overlayfs
# fallback). `additional_flags` is the documented distrobox-assemble key for
# extra container-manager flags (string_list, space-separated).
additional_flags="--cap-add sys_admin --device /dev/fuse"
# workspace = CRITICAL (edited notebooks + trained adapters live there — the
# resolver refuses to proceed unless bound; ALLOW_EPHEMERAL=1 escapes) and
# lives beside config/ and program/ under the box's own directory by default.
# /opt/config = the box's CONFIG SURFACE and nothing else (finetuning.cfg and
# jupyter_server_config.py, which is only on Jupyter's search path because
# JUPYTER_CONFIG_DIR names this directory) — never wiped, never overwritten.
# /opt/program = per-box PROGRAM DATA the installer never empties (the venv
# upper, logs/).
# /opt/program-cache = per-box PROGRAM CACHE the installer may empty on
# request (the per-box compute caches, the overlay work dirs, BOTH lanes since
# lane unification).
# Shared compute caches across ALL droste boxes are appended below (optional — remove to keep caches per-box).
volume="/home/kirobo/droste/data/finetuning/config:/opt/config /home/kirobo/droste/data/finetuning/program:/opt/program /home/kirobo/droste/caches/finetuning:/opt/program-cache /home/kirobo/droste/data/finetuning/workspace:/opt/workspace /home/kirobo/droste/compute-caches:/opt/caches"
